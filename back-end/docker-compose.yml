version: "3"
services:
    daily_diet_api:
        image: bitnami/postgresql
        ports:
            - 5432:5432
        environment:
            - POSTGRES_USER=docker
            - POSTGRES_PASSWORD=docker
            - POSTGRES_DB=daily_diet_api
